---
phase: 69-dockerization-model-baking
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/Dockerfile
  - backend/app/core/reranker.py
  - docker-compose.yml
autonomous: true
requirements:
  - MEM-DK-01
  - MEM-DK-02
  - MEM-DK-03
  - MEM-DK-04
  - MEM-DK-05
---

<objective>
Configure FastEmbed and FlashRank model caching directories to be isolated from database volumes, pre-download and bake these model weights during the Docker image building phase, and align backend Dockerfile builds using requirements.txt dependency resolution.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-dockerization-model-baking/69-CONTEXT.md
@backend/Dockerfile
@backend/app/core/reranker.py
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update RerankManager in reranker.py</name>
  <files>backend/app/core/reranker.py</files>
  <read_first>backend/app/core/reranker.py</read_first>
  <action>
    Modify `backend/app/core/reranker.py` inside `get_ranker`:
    - Read `FLASHRANK_CACHE_DIR` environment variable defaulting to `"/app/models/flashrank"`.
    - Pass this cache directory to the `Ranker` constructor:
      `cls._instance = Ranker(model_name=model_name, cache_dir=cache_dir)`
  </action>
  <verify>Ensure python syntax is correct.</verify>
  <acceptance_criteria>
    - FlashRank accepts custom cache_dir argument.
  </acceptance_criteria>
  <done>RerankManager cache_dir logic updated.</done>
</task>

<task type="auto">
  <name>Task 2: Align Dockerfile dependency resolution and pre-download models</name>
  <files>backend/Dockerfile</files>
  <read_first>backend/Dockerfile</read_first>
  <action>
    Modify `backend/Dockerfile`:
    - Expose `ENV FASTEMBED_CACHE_DIR=/app/models/fastembed` and `FLASHRANK_CACHE_DIR=/app/models/flashrank` environment variables.
    - Copy `requirements.txt` instead of `pyproject.toml` in `COPY requirements.txt ./`.
    - Install using `uv pip install --system -r requirements.txt`.
    - Bake models: Add `RUN python -c "from fastembed import TextEmbedding; TextEmbedding(model_name='sentence-transformers/all-MiniLM-L6-v2', cache_dir='/app/models/fastembed'); TextEmbedding(model_name='BAAI/bge-small-en-v1.5', cache_dir='/app/models/fastembed')"`
    - Add `RUN python -c "from flashrank import Ranker; Ranker(model_name='ms-marco-MiniLM-L-12-v2', cache_dir='/app/models/flashrank')"`
    - Update `RUN mkdir -p` line to also include `/app/models` in addition to `/app/data`.
  </action>
  <verify>Verify Dockerfile compiles and runs script layers cleanly.</verify>
  <acceptance_criteria>
    - Dockerfile contains dependency copying, cache directories configuration, and model downloading step during build time.
  </acceptance_criteria>
  <done>Dockerfile updated with model baking and aligned dependencies.</done>
</task>

<task type="auto">
  <name>Task 3: Update docker-compose environment variables</name>
  <files>docker-compose.yml</files>
  <read_first>docker-compose.yml</read_first>
  <action>
    Modify `docker-compose.yml` under `backend` service:
    - Expose environment variables `FASTEMBED_CACHE_DIR=/app/models/fastembed` and `FLASHRANK_CACHE_DIR=/app/models/flashrank`.
  </action>
  <verify>Syntax is valid YAML.</verify>
  <acceptance_criteria>
    - compose file specifies correct model cache parameters.
  </acceptance_criteria>
  <done>docker-compose updated with environment configurations.</done>
</task>

<task type="auto">
  <name>Task 4: Build Docker image and verify E2E integration</name>
  <files>docker-compose.yml</files>
  <read_first>docker-compose.yml</read_first>
  <action>
    Propose running a test image build of the backend container:
    ```bash
    docker compose build backend
    ```
    Verify it builds successfully and downloads models during the build phase.
  </action>
  <verify>Docker image builds without error.</verify>
  <acceptance_criteria>
    - Build finishes with exit code 0.
  </acceptance_criteria>
  <done>Docker build verification passed.</done>
</task>

</tasks>

<verification>
Verify build:
- [ ] Run `docker compose build backend` to confirm the backend service builds successfully.
- [ ] Run pytest backend tests to confirm everything still works locally.
</verification>

<success_criteria>
- Backend Docker container builds successfully.
- Models are pre-cached inside the container and loaded from local cache directory.
- Pytest tests are verified green.
</success_criteria>

<output>
After completion, create `.planning/phases/69-dockerization-model-baking/69-01-SUMMARY.md`
</output>
