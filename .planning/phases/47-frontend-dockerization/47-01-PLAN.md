---
phase: 47-frontend-dockerization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/next.config.ts
  - frontend/Dockerfile
  - frontend/.dockerignore
autonomous: true
requirements:
  - DKR-FE-01
  - DKR-FE-02
  - DKR-FE-03

must_haves:
  truths:
    - Next.config.ts is configured with output standalone mode.
    - Frontend Docker image compiles Next.js project successfully using multi-stage build.
    - Exposes port 3000 and boots cleanly on production runner.
  artifacts:
    - frontend/next.config.ts
    - frontend/Dockerfile
    - frontend/.dockerignore
---

<objective>
Dockerize the Next.js App Router frontend to produce a lightweight standalone production build running on Node.js.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/package.json
@frontend/next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author frontend/.dockerignore</name>
  <files>frontend/.dockerignore</files>
  <action>
    Create a new `.dockerignore` file in the `frontend/` directory:
    - Ignore `node_modules`, `.next`, `.git`, `.env.local`, `.env` and standard transient paths to keep build contexts clean.
  </action>
  <verify>
    Verify the file is created with the correct ignore list.
  </verify>
  <acceptance_criteria>
    - `frontend/.dockerignore` exists and contains standard ignore directories.
  </acceptance_criteria>
  <done>Created frontend/.dockerignore</done>
</task>

<task type="auto">
  <name>Task 2: Configure Standalone Output in Next.js</name>
  <files>frontend/next.config.ts</files>
  <read_first>frontend/next.config.ts</read_first>
  <action>
    Modify `frontend/next.config.ts` to enable `output: "standalone"`.
  </action>
  <verify>
    Ensure file updates correctly without TypeScript errors.
  </verify>
  <acceptance_criteria>
    - `next.config.ts` contains `output: "standalone"` parameter.
  </acceptance_criteria>
  <done>Configured Next.js output standalone option.</done>
</task>

<task type="auto">
  <name>Task 3: Author frontend/Dockerfile</name>
  <files>frontend/Dockerfile</files>
  <action>
    Create a multi-stage `Dockerfile` in the `frontend/` directory:
    - Install `pnpm` globally in builder stages.
    - Stage 1: Install dependencies using `pnpm install --frozen-lockfile` (or `pnpm fetch` / standard build).
    - Stage 2: Copy source files and build (`pnpm build`).
    - Stage 3: Setup minimal runner image with Node.js 20-alpine:
      - Create system user `nextjs` and group `nodejs`.
      - Copy standalone node package `.next/standalone` to run path.
      - Copy public and static assets: `.next/static` to `.next/standalone/.next/static` and `public` to `.next/standalone/public`.
      - Expose port 3000.
      - Boot script CMD: `node server.js` (which is standard for standalone Next.js builds).
  </action>
  <verify>
    Run a local build command. (We will skip executing this locally as Docker daemon is off, but build configuration will be fully reviewed).
  </verify>
  <acceptance_criteria>
    - `frontend/Dockerfile` exists.
    - Features multi-stage build optimizing standalone layout.
    - Uses non-root user for startup.
  </acceptance_criteria>
  <done>Created frontend/Dockerfile</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Review Dockerfile instructions for multi-stage correctness and standalone routing.
- If Docker daemon were active, we would build the image: `docker build -t document-rag-frontend ./frontend`
</verification>

<success_criteria>
- All tasks completed successfully.
</success_criteria>

<output>
Create `.planning/phases/47-frontend-dockerization/47-01-SUMMARY.md`
</output>
