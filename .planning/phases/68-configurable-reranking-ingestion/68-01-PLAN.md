---
phase: 68-configurable-reranking-ingestion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/main.py
  - backend/app/routes/query.py
  - backend/app/routes/upload.py
  - backend/app/core/chunker.py
autonomous: true
requirements:
  - MEM-CFG-01
  - MEM-CFG-02
---

<objective>
Configure the cross-encoder reranker to be completely optional via `ENABLE_RERANKING` environment variables, and optimize document ingestion memory using active garbage collection loops and route-level file size upload limits.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-configurable-reranking-ingestion/68-CONTEXT.md
@backend/main.py
@backend/app/routes/query.py
@backend/app/routes/upload.py
@backend/app/core/chunker.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Log startup message in main.py</name>
  <files>backend/main.py</files>
  <read_first>backend/main.py</read_first>
  <action>
    Modify `backend/main.py`:
    - Inside `lifespan(app: FastAPI)` function:
      Read the environment variable `os.getenv("ENABLE_RERANKING", "true")`.
      If it is set to `"false"`, print/log: `[Info] FlashRank reranking is disabled via environment variable.`
  </action>
  <verify>Verify startup hook triggers log when ENABLE_RERANKING=false is set.</verify>
  <acceptance_criteria>
    - Startup lifespan triggers the log warning correctly.
  </acceptance_criteria>
  <done>Lifespan initialization log added.</done>
</task>

<task type="auto">
  <name>Task 2: Implement dynamic reranking checks in query.py</name>
  <files>backend/app/routes/query.py</files>
  <read_first>backend/app/routes/query.py</read_first>
  <action>
    Modify `backend/app/routes/query.py` inside `retrieve_and_rerank_context`:
    - Read `ENABLE_RERANKING` env variable.
    - If `ENABLE_RERANKING` is `"false"`, bypass calling `RerankManager.get_ranker()`. Take `deduped_chunks[:top_k]` directly, set `reranking_ms = 0.0`, and return them.
  </action>
  <verify>Ensure `top_k` chunk slicing behaves correctly when reranking is bypassed.</verify>
  <acceptance_criteria>
    - No flashrank loading triggers if reranking is disabled.
  </acceptance_criteria>
  <done>Dynamic query reranker bypass implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce upload size limits in upload.py</name>
  <files>backend/app/routes/upload.py</files>
  <read_first>backend/app/routes/upload.py</read_first>
  <action>
    Modify `backend/app/routes/upload.py` inside the `upload_file` endpoint:
    - Compute `max_size_mb` from `MAX_UPLOAD_SIZE_MB` env variable (default `50`).
    - Resolve the size of `file: UploadFile`. If `file.size` is None, calculate by seeking and telling, then seeking back to 0.
    - If size is greater than `max_size_mb * 1024 * 1024`, raise `HTTPException(status_code=400, detail="File size exceeds the limit of {max_size_mb} MB.")`.
  </action>
  <verify>Run a test with a file size threshold override configured.</verify>
  <acceptance_criteria>
    - Upload files exceeding size threshold are rejected with HTTP 400.
  </acceptance_criteria>
  <done>Upload size limits enforced.</done>
</task>

<task type="auto">
  <name>Task 4: Enforce garbage collection in chunker.py</name>
  <files>backend/app/core/chunker.py</files>
  <read_first>backend/app/core/chunker.py</read_first>
  <action>
    Modify `backend/app/core/chunker.py` inside `DocumentChunker.split_documents`:
    - In the loop `for doc in docs:`:
      At the end of the loop body (after creating child documents for the page), explicitly run `del parent_chunks`.
      Import `gc` and call `gc.collect()`.
  </action>
  <verify>Check compilation syntax.</verify>
  <acceptance_criteria>
    - Ingestion loop cleans up parent chunk memory references and triggers garbage collection page-by-page.
  </acceptance_criteria>
  <done>Garbage collection optimizations integrated.</done>
</task>

<task type="auto">
  <name>Task 5: Run backend tests and verify functionality</name>
  <files>backend/tests/test_reranker.py</files>
  <read_first>backend/tests/test_reranker.py</read_first>
  <action>
    Run:
    ```bash
    uv run pytest
    ```
    If any tests fail because they assert reranker exists or require specific patches, update them.
    Create a new test inside `backend/tests/test_reranker.py` (or modify it) to verify that if `ENABLE_RERANKING=false`, the query route skips the reranker.
  </action>
  <verify>All 112 pytest unit tests pass cleanly.</verify>
  <acceptance_criteria>
    - Pytest suite is green.
  </acceptance_criteria>
  <done>All tests are verified green.</done>
</task>

</tasks>

<verification>
Verify backend test suite:
- [ ] Run `uv run pytest` to confirm all tests pass cleanly.
- [ ] Mock verify rate limits and file size rejections.
</verification>

<success_criteria>
- Ingestion runs successfully.
- Query endpoint respects `ENABLE_RERANKING` environment variable.
- Ingestion cleans up memory using gc page-by-page.
- Commit all changes inside `backend/` and root repository.
</success_criteria>

<output>
After completion, create `.planning/phases/68-configurable-reranking-ingestion/68-01-SUMMARY.md`
</output>
