---
phase: 34-theme-switching-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/components/theme-provider.tsx
  - frontend/src/app/layout.tsx
  - frontend/src/components/ThemeToggle.tsx
  - frontend/src/components/DashboardShell.tsx
  - frontend/src/components/ChatShell.tsx
autonomous: true
requirements:
  - FE-THEME-01
  - FE-THEME-02
user_setup: []

must_haves:
  truths:
    - "next-themes is installed and ThemeProvider is loaded in layout.tsx"
    - "ThemeToggle component cycles between light/dark theme values cleanly"
    - "ThemeToggle button is embedded in Dashboard and Chat shell headers"
  artifacts:
    - "frontend/src/components/ThemeToggle.tsx"
    - "frontend/src/components/theme-provider.tsx"
---

<objective>
Install next-themes, wire up ThemeProvider context, build the ThemeToggle component, and embed it inside Dashboard and Chat content headers.

Purpose: Provide user-controlled theme toggling between light, dark, and system themes.
Output: Integrated theme provider, custom ThemeToggle component, and updated page headers.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-theme-switching-integration/34-CONTEXT.md
@.planning/phases/34-theme-switching-integration/34-UI-SPEC.md
@frontend/src/app/layout.tsx
@frontend/src/components/DashboardShell.tsx
@frontend/src/components/ChatShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install next-themes dependency</name>
  <files>frontend/package.json</files>
  <read_first>frontend/package.json</read_first>
  <action>
    Run `pnpm add next-themes` inside the `frontend` folder to install the themes orchestration module.
  </action>
  <verify>
    Verify package.json contains next-themes in dependencies.
  </verify>
  <acceptance_criteria>
    - next-themes exists in package.json dependencies
  </acceptance_criteria>
  <done>next-themes module installed successfully</done>
</task>

<task type="auto">
  <name>Task 2: Build ThemeProvider wrapper component</name>
  <files>frontend/src/components/theme-provider.tsx</files>
  <read_first>frontend/src/app/layout.tsx</read_first>
  <action>
    [NEW] Create `frontend/src/components/theme-provider.tsx` client component:
    - Import ThemeProvider from next-themes.
    - Export custom ThemeProvider wrapping next-themes Provider with custom config:
      - attribute="class"
      - defaultTheme="system"
      - enableSystem
  </action>
  <verify>
    Verify theme-provider.tsx compiles cleanly.
  </verify>
  <acceptance_criteria>
    - theme-provider.tsx exists and exports ThemeProvider client component
  </acceptance_criteria>
  <done>ThemeProvider wrapper component written</done>
</task>

<task type="auto">
  <name>Task 3: Wrap root layout in ThemeProvider</name>
  <files>frontend/src/app/layout.tsx</files>
  <read_first>frontend/src/app/layout.tsx</read_first>
  <action>
    Modify `frontend/src/app/layout.tsx` to:
    - Import ThemeProvider from @/components/theme-provider.
    - Wrap the `{children}` elements of `RootLayout` in `<ThemeProvider>`.
  </action>
  <verify>
    Verify root layout compiles.
  </verify>
  <acceptance_criteria>
    - layout.tsx imports and wraps children in ThemeProvider
  </acceptance_criteria>
  <done>ThemeProvider configured in layout.tsx</done>
</task>

<task type="auto">
  <name>Task 4: Build ThemeToggle client component</name>
  <files>frontend/src/components/ThemeToggle.tsx</files>
  <read_first>frontend/src/components/ui/button.tsx</read_first>
  <action>
    [NEW] Create `frontend/src/components/ThemeToggle.tsx` client component:
    - Import useTheme from next-themes.
    - Import Sun, Moon from lucide-react.
    - Render a ghost icon Button of size 9x9.
    - Read `resolvedTheme` and toggle theme: if resolvedTheme === "dark", set to "light", else "dark".
    - Prevent layout shifts / hydration flashes by returning a skeleton button placeholder if not mounted.
  </action>
  <verify>
    Verify ThemeToggle.tsx matches UI-SPEC styling rules and loads icons.
  </verify>
  <acceptance_criteria>
    - ThemeToggle component is fully written and client-safe
  </acceptance_criteria>
  <done>ThemeToggle selector component created</done>
</task>

<task type="auto">
  <name>Task 5: Embed ThemeToggle in headers</name>
  <files>frontend/src/components/DashboardShell.tsx, frontend/src/components/ChatShell.tsx</files>
  <read_first>frontend/src/components/DashboardShell.tsx</read_first>
  <action>
    Update headers layouts to embed ThemeToggle:
    - In `DashboardShell.tsx`: Add `<ThemeToggle />` to the header toolbar (aligned right next to user username panel).
    - In `ChatShell.tsx`: Add `<ThemeToggle />` to the chat room toolbar (aligned right next to document selector button).
  </action>
  <verify>
    Verify shells compile cleanly. (Build step skipped as requested by the user).
  </verify>
  <acceptance_criteria>
    - both Shell components import and render ThemeToggle in headers
  </acceptance_criteria>
  <done>ThemeToggle integrated in main pages headers</done>
</task>

</tasks>

<verification>
Ensure dev compiles cleanly. (Build step skipped as requested by the user).
</verification>

<success_criteria>
- All tasks completed
- No compilation errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/34-theme-switching-integration/34-01-SUMMARY.md`
</output>
