---
phase: 48-docker-compose-orchestration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
autonomous: true
requirements:
  - DKR-CMP-01
  - DKR-CMP-02
  - DKR-CMP-03
  - DKR-CMP-04

must_haves:
  truths:
    - Root-level docker-compose.yml is created.
    - Defines frontend and backend services.
    - Mounts local backend persistent database/vectors.
    - Exposes ports 8000 and 3000 correctly.
  artifacts:
    - docker-compose.yml
---

<objective>
Orchestrate the backend and frontend services using Docker Compose with persistent storage mapping.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/Dockerfile
@frontend/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author root-level docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <action>
    Create a `docker-compose.yml` file in the root directory:
    - Version: '3.8' or omission (modern compose syntax).
    - Services:
      - `backend`:
        - Build context `./backend` and target `Dockerfile`.
        - Environment: Pass `GROQ_API_KEY` (allowing host forwarding), set `DATA_DIR=/app/data`.
        - Ports: Map `8000:8000`.
        - Volumes: Mount named volume `backend_data` to `/app/data`.
      - `frontend`:
        - Build context `./frontend` and target `Dockerfile`.
        - Environment: Set `NEXT_PUBLIC_API_URL=http://localhost:8000`.
        - Ports: Map `3000:3000`.
    - Volumes: Define `backend_data` named volume.
    - Networks: Define a common bridge network (optional but recommended, otherwise default is used).
  </action>
  <verify>
    Verify compose file syntax with a dry-run or validation command if possible.
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` exists at the root of the workspace.
    - Contains both backend and frontend services.
    - Declares backend_data volume.
  </acceptance_criteria>
  <done>Created docker-compose.yml</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Review the `docker-compose.yml` file layout.
- If Docker daemon were active, we would test running: `docker compose config` and `docker compose up --build -d`
</verification>

<success_criteria>
- Task completed successfully.
</success_criteria>

<output>
Create `.planning/phases/48-docker-compose-orchestration/48-01-SUMMARY.md`
</output>
