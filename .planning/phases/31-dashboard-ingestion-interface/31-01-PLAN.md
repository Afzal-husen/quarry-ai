---
phase: 31-dashboard-ingestion-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/DashboardShell.tsx
autonomous: true
requirements:
  - FE-DASH-01
  - FE-DASH-02
  - FE-DASH-03
  - FE-DASH-04
user_setup: []

must_haves:
  truths:
    - "frontend builds successfully"
    - "dashboard renders a collapsible left sidebar navigation"
    - "dragging files over the window displays the full-screen blurred dropzone overlay"
    - "table uses shadcn Table components and delete action opens a custom dialog confirmation"
  artifacts:
    - "frontend/src/components/DashboardShell.tsx"
  key_links:
    - "DashboardShell imports Table, Card, Badge, and Dialog components"
---

<objective>
Refactor DashboardShell.tsx to implement the collapsible left sidebar, visual page-wide drag-and-drop file upload target overlay, animated processing status badges, shadcn Table listing, and custom Dialog deletion confirmations.

Purpose: Build a premium document management and ingestion dashboard shell.
Output: Refactored DashboardShell.tsx file.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-dashboard-ingestion-interface/31-CONTEXT.md
@.planning/phases/31-dashboard-ingestion-interface/31-UI-SPEC.md
@frontend/src/components/DashboardShell.tsx
@frontend/src/components/UploadModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild page layout with collapsible left sidebar and cards</name>
  <files>frontend/src/components/DashboardShell.tsx</files>
  <read_first>frontend/src/components/DashboardShell.tsx</read_first>
  <action>
    Modify `frontend/src/components/DashboardShell.tsx` to:
    - Restructure layout to wrap content inside a horizontal split container (Sidebar left, Main viewport right).
    - Design a left collapsible sidebar: width `w-64` (expanded) transition to `w-16` (collapsed).
    - Sidebar must render a toggle button, Branding details, Dashboard / Chat feed navigation links, and User profile details with Sign Out trigger.
    - Rebuild metrics summaries (Total Documents, Indexed Chunks, Pending Ingestions) using shadcn Card primitives.
  </action>
  <verify>
    Verify DashboardShell imports Card elements and transitions sidebar layouts correctly.
  </verify>
  <acceptance_criteria>
    - DashboardShell contains state for sidebar collapse (e.g. collapsed/expanded widths)
    - DashboardShell compiles successfully
  </acceptance_criteria>
  <done>Sidebar navigation and stats cards layout completed</done>
</task>

<task type="auto">
  <name>Task 2: Build page-wide file drag-and-drop upload overlay</name>
  <files>frontend/src/components/DashboardShell.tsx</files>
  <read_first>frontend/src/components/DashboardShell.tsx</read_first>
  <action>
    Implement page-wide window drag listeners in `DashboardShell.tsx`:
    - Setup state `isDraggingOver`.
    - Register window event listeners for `dragenter`, `dragover`, `dragleave`, and `drop`.
    - Render a fullscreen blurred overlay (`bg-zinc-950/80 backdrop-blur-md border-4 border-dashed border-indigo-500/50 m-6 rounded-2xl z-50 flex flex-col justify-center items-center`) when dragging.
    - On drop, execute file type validation (allowed: PDF, DOCX) and size validation (max 50MB).
    - Trigger `apiPost('/upload', formData)` and register the resulting job_id to the polling queue list.
  </action>
  <verify>
    Verify that drag event bindings are registered in a useEffect hooks and cleanup is handled on unmount.
  </verify>
  <acceptance_criteria>
    - DashboardShell contains window drag listeners in useEffect hook
    - DashboardShell contains file validations checking types and sizes
  </acceptance_criteria>
  <done>Page-wide drag-and-drop file target overlay implemented</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Catalog table and add custom delete confirmation Dialog</name>
  <files>frontend/src/components/DashboardShell.tsx</files>
  <read_first>frontend/src/components/DashboardShell.tsx</read_first>
  <action>
    Update document catalog and actions in `DashboardShell.tsx`:
    - Replace the raw table with shadcn `Table` primitives (Table, TableHeader, TableRow, TableBody, TableCell).
    - Format job status indicators to use colored `Badge` badges (Complete: green, Failed: red, Processing: pulsing amber animate-pulse).
    - Rebuild the delete file trigger to open a shadcn `Dialog` (confirm delete action, cancel action) instead of browser `window.confirm`.
  </action>
  <verify>
    Verify that document list uses Table components and delete triggers set active document in state to open Dialog.
  </verify>
  <acceptance_criteria>
    - DashboardShell contains shadcn Dialog/AlertDialog confirm elements
    - Catalog renders badges representing document statuses
  </acceptance_criteria>
  <done>Catalog table refactored and custom confirmation dialog implemented</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `pnpm build` in the `frontend` folder to guarantee clean build compilation.
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Frontend builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/31-dashboard-ingestion-interface/31-01-SUMMARY.md`
</output>
