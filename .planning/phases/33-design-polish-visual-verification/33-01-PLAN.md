---
phase: 33-design-polish-visual-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/globals.css
  - frontend/src/components/DashboardShell.tsx
  - frontend/src/components/ChatShell.tsx
autonomous: true
requirements:
  - FE-POLISH-01
user_setup: []

must_haves:
  truths:
    - "custom scrollbar classes are defined in globals.css"
    - "sidebar and reference panels use smooth ease-in-out transitions duration classes"
    - "buttons and input elements map focus rings to indigo"
  artifacts:
    - "frontend/src/app/globals.css"
---

<objective>
Refactor globals.css, DashboardShell.tsx, and ChatShell.tsx to implement custom scrollbars, smooth ease-in-out transition-all animations, and focus ring outlines.

Purpose: Provide a polished RAG application layout.
Output: Refactored globals.css, DashboardShell.tsx, and ChatShell.tsx files.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-design-polish-visual-verification/33-CONTEXT.md
@.planning/phases/33-design-polish-visual-verification/33-UI-SPEC.md
@frontend/src/app/globals.css
@frontend/src/components/DashboardShell.tsx
@frontend/src/components/ChatShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Custom Scrollbars utility to globals.css</name>
  <files>frontend/src/app/globals.css</files>
  <read_first>frontend/src/app/globals.css</read_first>
  <action>
    Modify `frontend/src/app/globals.css` to append custom webkit-scrollbar styling rules for dark mode targets:
    - Width and height set to 6px.
    - Track set to transparent background.
    - Thumb set to Zinc-800 color (e.g. `oklch(0.269 0 0)`) with rounded borders.
    - Thumb hover state set to Zinc-700 color (e.g. `oklch(0.371 0 0)`).
  </action>
  <verify>
    Verify globals.css contains scrollbar classes at the end.
  </verify>
  <acceptance_criteria>
    - globals.css contains webkit-scrollbar utility selectors
  </acceptance_criteria>
  <done>Custom scrollbars rules added to globals.css</done>
</task>

<task type="auto">
  <name>Task 2: Standardize sidebar transition timers and ease curves</name>
  <files>frontend/src/components/DashboardShell.tsx, frontend/src/components/ChatShell.tsx</files>
  <read_first>frontend/src/components/DashboardShell.tsx</read_first>
  <action>
    Review and update transition layout classes inside sidebars of `DashboardShell.tsx` and `ChatShell.tsx`:
    - Set sidebar panel element classes to use `transition-all duration-300 ease-in-out` curves.
    - Confirm toggle button states and icons transitions match.
  </action>
  <verify>
    Verify sidebars contain transition-all duration-300 ease-in-out classes.
  </verify>
  <acceptance_criteria>
    - sidebar containers contain transition-all, duration-300, and ease-in-out classes
  </acceptance_criteria>
  <done>Sidebar expand/collapse motion curves standardized</done>
</task>

<task type="auto">
  <name>Task 3: Refine outlines and responsive boundaries</name>
  <files>frontend/src/components/DashboardShell.tsx, frontend/src/components/ChatShell.tsx</files>
  <read_first>frontend/src/components/DashboardShell.tsx</read_first>
  <action>
    Verify focus rings and responsive boundaries:
    - Align button focus classes to map to Indigo rings (`focus-visible:ring-indigo-500` or standard input ring hooks).
    - Review text area/inputs focus boundaries.
    - Check responsive layout tables wrappers (`overflow-x-auto`) to prevent horizontal screen leaks on narrow screens.
  </action>
  <verify>
    Verify focus visible classes are applied to interactive buttons and selectors.
  </verify>
  <acceptance_criteria>
    - interactive buttons use focus-visible and ring-indigo classes
  </acceptance_criteria>
  <done>Focus outline alignments and responsive grid safety completed</done>
</task>

</tasks>

<verification>
Ensure compile works. (Build step skipped as requested by the user).
</verification>

<success_criteria>
- All tasks completed
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/33-design-polish-visual-verification/33-01-SUMMARY.md`
</output>
