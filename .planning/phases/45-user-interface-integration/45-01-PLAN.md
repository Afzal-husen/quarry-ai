---
phase: 45-user-interface-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/DashboardShell.tsx
  - frontend/src/components/PreviewModal.tsx
autonomous: true
requirements:
  - SUM-UI-01
  - SUM-UI-02
  - SUM-UI-03

must_haves:
  truths:
    - Document grid cards display a badge indicating summary state and render a snippet of the summary in their descriptions.
    - The document preview modal contains a split view with the document preview on the left and a toggleable AI summary sidebar on the right.
    - The summary sidebar shows a "Regenerate" button which calls the backend regeneration API.
    - The summary status is polled every 2.5 seconds when pending, updating the UI dynamically.
  artifacts: []
  key_links:
    - PreviewModal.tsx imports parseMarkdown from lib/markdown-parser.tsx and apiPost from lib/api-client.ts.
---

<objective>
Build a sleek, premium frontend integration for document summaries with split-pane preview layout, status badges, and manual async regeneration.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/DashboardShell.tsx
@frontend/src/components/PreviewModal.tsx
@frontend/src/lib/markdown-parser.tsx
@frontend/src/lib/api-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DashboardShell.tsx Document Cards</name>
  <files>frontend/src/components/DashboardShell.tsx</files>
  <read_first>frontend/src/components/DashboardShell.tsx</read_first>
  <action>
    - Update the `DocumentItem` interface to include optional fields: `summary?: string; summary_status?: string;`.
    - In `documents.map` inside the JSX render:
      - Render a summary status badge next to the document status badge (Complete/Failed):
        - If `doc.summary_status === "completed"`, render an indigo `"Digest"` badge.
        - If `doc.summary_status === "pending"`, render a pulsing amber `"Digest pending"` badge.
        - If `doc.summary_status === "failed"`, render a gray `"No Digest"` badge.
      - Update the description line: if `doc.summary` exists, strip Markdown markers (`#`, `*`, `` ` ``), trim it, and render a line-clamp-2 snippet. Otherwise, fall back to the default `isPdf ? "PDF Document" : "Word Document"`.
  </action>
  <verify>
    Verify that the TypeScript compiler runs without errors.
  </verify>
  <acceptance_criteria>
    - DocumentItem interface contains summary and summary_status fields.
    - Document card displays the summary status badge and description snippet correctly.
  </acceptance_criteria>
  <done>Dashboard document card list updated with summary details.</done>
</task>

<task type="auto">
  <name>Task 2: Update PreviewModal.tsx with Summary Split View</name>
  <files>frontend/src/components/PreviewModal.tsx</files>
  <read_first>frontend/src/components/PreviewModal.tsx</read_first>
  <action>
    - Update `DocumentItem` interface in `PreviewModal.tsx` to include `summary?: string; summary_status?: string;`.
    - Add state variables:
      - `showSummary` (boolean, default true).
      - `summary` (string, default "").
      - `summaryStatus` (string, default "pending").
      - `regenerating` (boolean, default false).
    - Implement a `useEffect` hook to load initial summary values when the modal opens and a document is selected:
      - If `doc.summary_status` is `"pending"`, start a 2.5 second polling interval fetching `GET /documents/{document_id}/summary` using `apiGet`. Stop polling once the status becomes `"completed"` or `"failed"`.
    - Implement `handleRegenerate` which:
      - Calls `apiPost(f"/documents/{document.document_id}/summary/regenerate")`.
      - Updates `summaryStatus` to `"pending"`.
      - Starts/resumes the polling loop.
      - Displays toast notices on success or failure.
    - Add a toggle button in the header bar with a FileText icon to toggle `showSummary` panel.
    - Update layout container in JSX:
      - Wrap the document viewport in a flex row container.
      - Show the original document content on the left (flex-1).
      - Render the summary panel on the right (width 80 or 96, border-l) when `showSummary` is true.
      - Use `parseMarkdown` from `../lib/markdown-parser` to render the summary markdown text inside the right panel.
      - Render regeneration button, progress spinners, or error status details depending on `summaryStatus`.
  </action>
  <verify>
    Ensure TypeScript builds correctly and tests pass.
  </verify>
  <acceptance_criteria>
    - PreviewModal.tsx compiles successfully.
    - Toggle button, split-pane layout, markdown parsing, and regeneration triggering work as designed.
  </acceptance_criteria>
  <done>PreviewModal split view and regeneration fully implemented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run frontend tests: `pnpm test` or `npm run test` inside the frontend directory.
- Verify page compilation and check for console/lint errors.
</verification>

<success_criteria>
- All tasks completed.
- Split-pane summary view is responsive and toggleable.
- Dynamic polling loop handles async summarization feedback smoothly.
- Manual re-summarization works on user request.
</success_criteria>

<output>
After completion, create `.planning/phases/45-user-interface-integration/45-01-SUMMARY.md`
</output>
