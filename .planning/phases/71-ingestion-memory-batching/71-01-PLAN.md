---
phase: 71-ingestion-memory-batching
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/vectorstore.py
autonomous: true
requirements:
  - MEM-BATCH-01
  - MEM-BATCH-02
---

<objective>
Optimize memory utilization during document indexing by loading and adding documents in batches of 100 to the Chroma vector store, triggering garbage collection after each batch insertion.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-ingestion-memory-batching/71-CONTEXT.md
@backend/app/core/vectorstore.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Batch document insertion in vectorstore.py</name>
  <files>backend/app/core/vectorstore.py</files>
  <read_first>backend/app/core/vectorstore.py</read_first>
  <action>
    Modify `backend/app/core/vectorstore.py` inside `index_document`:
    - Replace the `vectorstore = Chroma.from_documents(...)` call.
    - Instantiate an empty `vectorstore = Chroma(persist_directory=str(db_path), embedding_function=embeddings)`
    - Loop over the list `documents` in steps of `BATCH_SIZE = 100`.
    - Inside the loop, run `vectorstore.add_documents(documents=batch)`.
    - Run `import gc; gc.collect()` after each batch is added.
  </action>
  <verify>Ensure correct python indentation and try/except scope.</verify>
  <done>Iterative batch document insertion implemented.</done>
</task>

</tasks>

<verification>
Verify build:
- [ ] Run pytest backend tests to confirm the vector store integration tests pass and documents index successfully.
</verification>

<success_criteria>
- pytest tests pass.
- Documents are indexed in batches of 100, checking correct SQLite database persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/71-ingestion-memory-batching/71-01-SUMMARY.md`
</output>
