---
phase: 29-shadcn-ui-setup-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/app/globals.css
  - frontend/src/app/layout.tsx
autonomous: true
requirements:
  - FE-SETUP-01
  - FE-SETUP-02
user_setup: []

must_haves:
  truths:
    - "frontend builds cleanly without compile or typescript errors"
    - "shadcn/ui primitive components exist under components/ui/"
  artifacts:
    - "frontend/components.json"
    - "frontend/src/components/ui/button.tsx"
    - "frontend/src/components/ui/card.tsx"
    - "frontend/src/components/ui/sidebar.tsx"
  key_links:
    - "globals.css is linked in layout.tsx"
---

<objective>
Initialize the shadcn/ui library, install necessary primitive components, configure OKLCH Indigo/Zinc theme values using Tailwind CSS v4, and integrate the Sonner toaster globally.

Purpose: Set up the design system foundation for rebuilding the web application screens.
Output: components.json configuration, installed shadcn components, tailwind custom theme definitions, and global toaster setup.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-shadcn-ui-setup-foundations/29-CONTEXT.md
@.planning/phases/29-shadcn-ui-setup-foundations/29-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add core UI package dependencies</name>
  <files>frontend/package.json</files>
  <read_first>frontend/package.json</read_first>
  <action>
    Run `pnpm add clsx tailwind-merge lucide-react sonner` inside the `frontend` directory to install className utility tools, iconography, and notifications toaster.
  </action>
  <verify>
    Check that `package.json` dependencies block contains "clsx", "tailwind-merge", "lucide-react", and "sonner".
  </verify>
  <acceptance_criteria>
    - package.json contains "sonner" dependency
    - package.json contains "lucide-react" dependency
  </acceptance_criteria>
  <done>Dependencies installed in package.json</done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui configuration</name>
  <files>frontend/components.json</files>
  <read_first>frontend/package.json</read_first>
  <action>
    Run `pnpm dlx shadcn@latest init --defaults` in the `frontend` directory to initialize shadcn configurations using the default Base Nova preset (Next.js template).
  </action>
  <verify>
    Check that `frontend/components.json` is created at the frontend root.
  </verify>
  <acceptance_criteria>
    - file frontend/components.json exists
    - components.json contains the alias configurations matching "@/*" imports
  </acceptance_criteria>
  <done>components.json created at frontend root</done>
</task>

<task type="auto">
  <name>Task 3: Install primitive shadcn/ui components</name>
  <files>
    frontend/src/components/ui/button.tsx,
    frontend/src/components/ui/card.tsx,
    frontend/src/components/ui/sidebar.tsx,
    frontend/src/components/ui/dialog.tsx,
    frontend/src/components/ui/input.tsx,
    frontend/src/components/ui/label.tsx,
    frontend/src/components/ui/form.tsx,
    frontend/src/components/ui/table.tsx,
    frontend/src/components/ui/tabs.tsx,
    frontend/src/components/ui/popover.tsx,
    frontend/src/components/ui/badge.tsx,
    frontend/src/components/ui/separator.tsx,
    frontend/src/components/ui/skeleton.tsx
  </files>
  <read_first>frontend/components.json</read_first>
  <action>
    Run the following command in the `frontend` directory:
    `pnpm dlx shadcn@latest add button card input label form table tabs sidebar dialog popover badge separator skeleton -y`
    to add all requested visual primitives to the components folder.
  </action>
  <verify>
    Verify that components/ui folder contains button.tsx, card.tsx, sidebar.tsx, form.tsx, and other files.
  </verify>
  <acceptance_criteria>
    - file frontend/src/components/ui/button.tsx exists
    - file frontend/src/components/ui/sidebar.tsx exists
    - file frontend/src/components/ui/card.tsx exists
  </acceptance_criteria>
  <done>Primitive UI component files successfully installed</done>
</task>

<task type="auto">
  <name>Task 4: Configure OKLCH Zinc/Indigo theme variables in globals.css</name>
  <files>frontend/src/app/globals.css</files>
  <read_first>frontend/src/app/globals.css</read_first>
  <action>
    Edit `frontend/src/app/globals.css` to define the OKLCH Zinc/Indigo custom color properties within the `@theme inline` block for Tailwind CSS v4. Ensure all state colors (active, borders, focus highlights) are correctly mapped using relative OKLCH colors, maintaining strict readability contrast ratios (>= 4.5:1).
  </action>
  <verify>
    Open globals.css and check that `@theme inline` contains OKLCH color definitions.
  </verify>
  <acceptance_criteria>
    - globals.css contains custom color values using oklch() colors mapping
  </acceptance_criteria>
  <done>globals.css configured with theme variables</done>
</task>

<task type="auto">
  <name>Task 5: Integrate global Sonner toaster in layout.tsx</name>
  <files>frontend/src/app/layout.tsx</files>
  <read_first>frontend/src/app/layout.tsx</read_first>
  <action>
    Edit `frontend/src/app/layout.tsx` to import and inject the `<Toaster />` component from `sonner` inside the `RootLayout` body block. This ensures that toast alerts work seamlessly on all pages.
  </action>
  <verify>
    Check that layout.tsx contains `import { Toaster } from "sonner"` and `<Toaster />` is rendered.
  </verify>
  <acceptance_criteria>
    - layout.tsx imports Toaster from "sonner"
    - layout.tsx renders <Toaster /> inside the body tag
  </acceptance_criteria>
  <done>Toaster integrated into root layout</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `pnpm build` in the `frontend` folder to guarantee clean build compilation.
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Frontend builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/29-shadcn-ui-setup-foundations/29-01-SUMMARY.md`
</output>
