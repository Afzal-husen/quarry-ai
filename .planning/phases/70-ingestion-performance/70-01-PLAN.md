---
phase: 70-ingestion-performance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/chunker.py
  - backend/Dockerfile
  - docker-compose.yml
autonomous: true
requirements:
  - GC-RELOC-01
  - GC-RELOC-02
  - ONNX-THREADS-01
  - ONNX-THREADS-02
---

<objective>
Optimize ingestion performance and prevent event loop/GIL starvation by relocating GC calls to the end of document splitting and constraining thread pool sizes for ONNX and BLAS execution.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-ingestion-performance/70-CONTEXT.md
@backend/app/core/chunker.py
@backend/Dockerfile
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate GC in chunker.py</name>
  <files>backend/app/core/chunker.py</files>
  <read_first>backend/app/core/chunker.py</read_first>
  <action>
    Modify `backend/app/core/chunker.py` inside `split_documents`:
    - Remove `import gc; gc.collect()` from inside the page loop.
    - Add `import gc; gc.collect()` just before returning `child_docs` at the end of the method.
  </action>
  <verify>Ensure correct indentation and syntax.</verify>
  <done>gc.collect() relocated outside loop.</done>
</task>

<task type="auto">
  <name>Task 2: Configure thread pool variables in Dockerfile</name>
  <files>backend/Dockerfile</files>
  <read_first>backend/Dockerfile</read_first>
  <action>
    Modify `backend/Dockerfile`:
    - Add `OMP_NUM_THREADS=1`, `MKL_NUM_THREADS=1`, and `OPENBLAS_NUM_THREADS=1` to the environment variables block under `ENV`.
  </action>
  <verify>Syntax validation passes.</verify>
  <done>Thread limits exposed in Dockerfile.</done>
</task>

<task type="auto">
  <name>Task 3: Configure thread pool variables in docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <read_first>docker-compose.yml</read_first>
  <action>
    Modify `docker-compose.yml`:
    - Add `OMP_NUM_THREADS=1`, `MKL_NUM_THREADS=1`, and `OPENBLAS_NUM_THREADS=1` to the `backend` environment parameters.
  </action>
  <verify>YAML syntax is valid.</verify>
  <done>Thread limits exposed in docker-compose.</done>
</task>

</tasks>

<verification>
Verify build:
- [ ] Run pytest backend tests to confirm the chunker still operates properly and yields chunks.
</verification>

<success_criteria>
- pytest tests are verified green.
- GC is only triggered once at the end of document ingestion.
</success_criteria>

<output>
After completion, create `.planning/phases/70-ingestion-performance/70-01-SUMMARY.md`
</output>
