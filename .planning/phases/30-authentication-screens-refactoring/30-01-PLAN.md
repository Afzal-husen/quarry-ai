---
phase: 30-authentication-screens-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/login/page.tsx
  - frontend/src/app/register/page.tsx
autonomous: true
requirements:
  - FE-AUTH-01
  - FE-AUTH-02
user_setup: []

must_haves:
  truths:
    - "frontend builds successfully"
    - "login and register pages present the Split Hero layout"
    - "forms reject invalid inputs client-side via react-hook-form"
  artifacts:
    - "frontend/src/app/login/page.tsx"
    - "frontend/src/app/register/page.tsx"
  key_links:
    - "pages import validation schemas from zod"
---

<objective>
Refactor the /login and /register route screens to present a premium Split Hero Layout (visual grid left panel, card form right panel) using shadcn UI components (Card, Input, Label, Button), react-hook-form validation, and Sonner notifications.

Purpose: Build a modern, high-fidelity auth entry flow.
Output: Refactored login/page.tsx and register/page.tsx files.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-authentication-screens-refactoring/30-CONTEXT.md
@.planning/phases/30-authentication-screens-refactoring/30-UI-SPEC.md
@frontend/src/app/login/page.tsx
@frontend/src/app/register/page.tsx
@frontend/src/app/actions/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Login page to Split Hero layout with Zod validations</name>
  <files>frontend/src/app/login/page.tsx</files>
  <read_first>frontend/src/app/login/page.tsx</read_first>
  <action>
    Modify `frontend/src/app/login/page.tsx` to:
    - Implement a two-column split grid layout (visible left branding panel on md+ screens, centered right card form panel).
    - Design a premium left branding panel with absolute position blurred radial highlights (using OKLCH Indigo colors), title text, subtitle, and list of platform values.
    - Structure the login card using shadcn Card, Input, Label, and Button primitives.
    - Wire react-hook-form validation with a Zod login schema (checking username >= 3 chars, password >= 6 chars).
    - Map form onSubmit to manually invoke `loginAction` within a React transition, and bind Sonner toast notifications + form warning banners to trigger in case of error responses.
  </action>
  <verify>
    Verify that login/page.tsx compiles correctly and imports `useForm`, `zodResolver`, `toast`, and standard card blocks.
  </verify>
  <acceptance_criteria>
    - file frontend/src/app/login/page.tsx contains Zod schema resolver setup
    - login/page.tsx imports Toaster / toast notifications from "sonner"
  </acceptance_criteria>
  <done>Login page refactored with Split Hero layout and Zod schema validations</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Register page to Split Hero layout with Zod validations</name>
  <files>frontend/src/app/register/page.tsx</files>
  <read_first>frontend/src/app/register/page.tsx</read_first>
  <action>
    Modify `frontend/src/app/register/page.tsx` to:
    - Implement matching Split Hero layout (reusing left brand highlights/description elements).
    - Structure the register card using shadcn Card, Input, Label, and Button primitives.
    - Wire react-hook-form validation with a Zod register schema (username >= 3 chars, password >= 6 chars, password === confirmPassword).
    - Map onSubmit to trigger `signupAction` inside a React transition, capturing state changes to fire Sonner alerts and handle errors.
  </action>
  <verify>
    Verify that register/page.tsx compiles correctly and contains the matching split column structure.
  </verify>
  <acceptance_criteria>
    - file frontend/src/app/register/page.tsx contains confirmPassword validation logic
  </acceptance_criteria>
  <done>Register page refactored with Split Hero layout and Zod validations</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `pnpm build` in the `frontend` folder to guarantee clean build compilation.
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Frontend builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/30-authentication-screens-refactoring/30-01-SUMMARY.md`
</output>
