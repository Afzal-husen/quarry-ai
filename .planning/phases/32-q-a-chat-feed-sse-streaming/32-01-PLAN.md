---
phase: 32-q-a-chat-feed-sse-streaming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ChatShell.tsx
autonomous: true
requirements:
  - FE-CHAT-01
  - FE-CHAT-02
  - FE-CHAT-03
user_setup: []

must_haves:
  truths:
    - "frontend builds successfully"
    - "chat screen renders the aligned collapsible navigation sidebar on the far left"
    - "secondary inner sidebar lists conversation threads"
    - "active streaming assistant message shows a blinking caret indicator"
    - "deleting thread opens a custom confirmation Dialog modal"
  artifacts:
    - "frontend/src/components/ChatShell.tsx"
  key_links:
    - "ChatShell imports Dialog, Badge, and Button components"
---

<objective>
Refactor ChatShell.tsx to implement the double sidebar layout (aligned left collapsible nav, secondary thread listing), blinking typewriter caret cursors, glassmorphism citations, and Dialog thread deletion confirmations.

Purpose: Build a premium chat screen with real-time SSE typewriter streaming.
Output: Refactored ChatShell.tsx file.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-q-a-chat-feed-sse-streaming/32-CONTEXT.md
@.planning/phases/32-q-a-chat-feed-sse-streaming/32-UI-SPEC.md
@frontend/src/components/ChatShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement double sidebar layout in ChatShell</name>
  <files>frontend/src/components/ChatShell.tsx</files>
  <read_first>frontend/src/components/ChatShell.tsx</read_first>
  <action>
    Modify `frontend/src/components/ChatShell.tsx` to:
    - Add states for collapsible sidebar (width transition `w-64` / `w-16`).
    - Implement the collapsible navigation sidebar on the far left, using the exact same visual styling, branding details, and user profile/logout actions as the dashboard layout.
    - Render the Chat History session list as a secondary inner sidebar (`w-72`) next to it, displaying the "New Chat" button and the active threads list.
  </action>
  <verify>
    Verify ChatShell renders both the collapsible nav sidebar and the thread list side-by-side.
  </verify>
  <acceptance_criteria>
    - ChatShell contains isSidebarCollapsed state
    - Sidebar elements align with Dashboard sidebar visuals
  </acceptance_criteria>
  <done>Sidebar navigation and double sidebar layout completed</done>
</task>

<task type="auto">
  <name>Task 2: Add typewriter caret streaming animations</name>
  <files>frontend/src/components/ChatShell.tsx</files>
  <read_first>frontend/src/components/ChatShell.tsx</read_first>
  <action>
    Update message bubbles and typewriter streaming in `ChatShell.tsx`:
    - Format user message bubbles with a clean background.
    - Format assistant message bubbles with a transparent layout, Spark header icon, and hover citation badges.
    - When SSE streaming is active (i.e. loading is true for the latest message), append a blinking text caret cursor `▋` with a pulsing animation `animate-pulse text-indigo-400` to the assistant bubble content.
  </action>
  <verify>
    Verify that blinking caret character is appended only to the active streaming message when query loading is active.
  </verify>
  <acceptance_criteria>
    - Message logs display the blinking caret character during stream loading
  </acceptance_criteria>
  <done>Typewriter caret streaming animations implemented</done>
</task>

<task type="auto">
  <name>Task 3: Refactor citations hover cards and dialog deletions</name>
  <files>frontend/src/components/ChatShell.tsx</files>
  <read_first>frontend/src/components/ChatShell.tsx</read_first>
  <action>
    Update deletion confirmation dialogs and popover styles in `ChatShell.tsx`:
    - Replace the native `window.confirm` for thread deletions with a beautiful custom Dialog confirm card.
    - Refactor target context document dropdown lists to match the Zinc/Indigo design palette.
    - Re-style CitationBadge hover popovers to use a modern, glassmorphism container backdrop.
  </action>
  <verify>
    Verify that deleting a session sets target session in state to open the confirm Dialog.
  </verify>
  <acceptance_criteria>
    - ChatShell imports Dialog elements to handle confirmation prompts
    - Citation popovers use glassmorphic backdrops
  </acceptance_criteria>
  <done>Citation cards refactored and custom confirmation dialog implemented</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `pnpm build` in the `frontend` folder to guarantee clean build compilation.
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Frontend builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/32-q-a-chat-feed-sse-streaming/32-01-SUMMARY.md`
</output>
