---
phase: 35-contrast-auditing-color-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/DashboardShell.tsx
  - frontend/src/components/ChatShell.tsx
autonomous: true
requirements:
  - FE-THEME-03
user_setup: []

must_haves:
  truths:
    - "DashboardShell has no hardcoded dark backgrounds on container elements"
    - "ChatShell uses bg-background, text-foreground, and border-border semantic classes"
  artifacts:
    - "frontend/src/components/DashboardShell.tsx"
    - "frontend/src/components/ChatShell.tsx"
---

<objective>
Refactor DashboardShell.tsx and ChatShell.tsx to replace hardcoded dark colors (zinc-950/900/800) with dynamic Tailwind CSS semantic theme variables.

Purpose: Fix light mode to render dynamic white backgrounds and dark mode to render dynamic dark backgrounds.
Output: Refactored DashboardShell.tsx and ChatShell.tsx files.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-contrast-auditing-color-polish/35-CONTEXT.md
@.planning/phases/35-contrast-auditing-color-polish/35-UI-SPEC.md
@frontend/src/components/DashboardShell.tsx
@frontend/src/components/ChatShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded colors in DashboardShell.tsx</name>
  <files>frontend/src/components/DashboardShell.tsx</files>
  <read_first>frontend/src/components/DashboardShell.tsx</read_first>
  <action>
    Convert all hardcoded color classes in `DashboardShell.tsx`:
    - Replace root `bg-zinc-950` layout classes with `bg-background`.
    - Replace main and header container `bg-zinc-950` classes with `bg-background` or `bg-background/40`.
    - Replace card elements `bg-zinc-950/50` backgrounds with `bg-card`.
    - Replace border lines `border-zinc-900` / `border-zinc-800` with `border-border`.
    - Replace text variables `text-zinc-50` / `text-zinc-100` / `text-zinc-200` with `text-foreground`.
    - Replace text variables `text-zinc-400` / `text-zinc-500` with `text-muted-foreground`.
    - Replace button hovers `hover:bg-zinc-900` with `hover:bg-accent hover:text-accent-foreground`.
  </action>
  <verify>
    Verify DashboardShell imports compile and use background/foreground/border/card classes.
  </verify>
  <done>DashboardShell refactored to use dynamic theme variables</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded colors in ChatShell.tsx</name>
  <files>frontend/src/components/ChatShell.tsx</files>
  <read_first>frontend/src/components/ChatShell.tsx</read_first>
  <action>
    Convert all hardcoded color classes in `ChatShell.tsx`:
    - Replace root `bg-zinc-950` layout classes with `bg-background`.
    - Replace header container `bg-zinc-950/40` classes with `bg-background/40`.
    - Replace chat log main backdrop `bg-zinc-950` with `bg-background`.
    - Replace border lines `border-zinc-900` / `border-zinc-800` with `border-border`.
    - Replace text variables `text-zinc-50` / `text-zinc-100` / `text-zinc-200` with `text-foreground`.
    - Replace text variables `text-zinc-400` / `text-zinc-500` with `text-muted-foreground`.
    - Replace list overlays or options dialog elements `bg-zinc-950` / `bg-zinc-900` with `bg-card` or `bg-muted`.
    - Replace button hovers `hover:bg-zinc-900` with `hover:bg-accent hover:text-accent-foreground`.
  </action>
  <verify>
    Verify ChatShell compiles cleanly. (Build step skipped as requested by the user).
  </verify>
  <done>ChatShell refactored to use dynamic theme variables</done>
</task>

</tasks>

<verification>
Ensure dev compiles cleanly. (Build step skipped as requested by the user).
</verification>

<success_criteria>
- All tasks completed
- No compilation errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/35-contrast-auditing-color-polish/35-01-SUMMARY.md`
</output>
